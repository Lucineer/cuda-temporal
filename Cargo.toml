[package]
name = "cuda-temporal"
version = "0.1.0"
edition = "2021"
description = "Temporal reasoning — time-aware agent logic with intervals, causality, deadlines, and scheduling"

[dependencies]
serde = { version = "1", features = ["derive"] }
